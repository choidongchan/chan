# PLAYON 자동 배포 가이드 (인터넷에 올리기)

> ⚠️ 공개 URL로 올리면 인터넷 누구나 접속할 수 있습니다. **반드시 관리자 비밀번호(ADMIN_PW)를 바꾸세요.**
> 실제 매장 영업용은 매장 서버에서 `start.bat`로 로컬 실행하는 것을 권장합니다. (데이터/속도/보안)

## 최초 1회 연결 (약 5분) — 이후로는 완전 자동

Render.com은 무료로 Node 서버를 올릴 수 있고, GitHub와 연결하면 **push할 때마다 자동 재배포**됩니다.

1. https://render.com 접속 → **GitHub 계정으로 가입/로그인**
2. **New +** → **Blueprint** 클릭
3. 저장소 **`choidongchan/chan`** 선택 → Render가 `render.yaml`을 자동 인식
4. **ADMIN_PW** 값에 원하는 관리자 비밀번호 입력 → **Apply**
5. 1~2분 뒤 배포 완료 → `https://playon-xxxx.onrender.com` 주소가 생김

## 이후 (자동)

제가 코드를 고쳐서 push하면 → Render가 **자동으로 감지해 재배포**합니다.
사장님은 그 주소만 새로고침하면 최신 버전이에요. 더 이상 아무것도 안 하셔도 됩니다.

## 참고
- 무료 플랜은 접속이 없으면 잠자기 모드가 되어, 다시 열 때 30초쯤 걸릴 수 있어요.
- 무료 플랜은 재배포 시 DB(playon.db)가 초기화됩니다. **데모/미리보기용**입니다.
- 실운영(회원/매출 영구 저장, 좌석 PC 연동)은 **매장 로컬 서버**에서 돌리세요.
